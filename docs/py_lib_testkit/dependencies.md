# Dependencies

Published testkit dependencies cover runtime helpers and test-facing libraries. `py-lib-policy` is repository verification only and belongs to the development dependency group, not the built package runtime metadata.
