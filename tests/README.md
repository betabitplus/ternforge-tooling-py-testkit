# Tests

The test suite covers the public `py_lib_testkit` contract, configuration discovery, VCR helpers, console utilities, image handling, and retained legacy behavior.

Run all tests with:

```bash
uv run pytest
```
